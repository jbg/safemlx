diff --git a/mlx/backend/metal/kernels/quantized.metal b/mlx/backend/metal/kernels/quantized.metal
--- a/mlx/backend/metal/kernels/quantized.metal
+++ b/mlx/backend/metal/kernels/quantized.metal
@@ -187,7 +187,15 @@
   instantiate_quantized_batched(affine_qmv_fast, type, 16, 6, 0) \
   instantiate_quantized_batched(affine_qmv_fast, type, 16, 6, 1) \
   instantiate_quantized_batched(affine_qmv, type, 16, 6, 0) \
-  instantiate_quantized_batched(affine_qmv, type, 16, 6, 1)
+  instantiate_quantized_batched(affine_qmv, type, 16, 6, 1) \
+  instantiate_quantized_aligned_batched(affine_qmm_t, type, 16, 6, true, 0) \
+  instantiate_quantized_aligned_batched(affine_qmm_t, type, 16, 6, true, 1) \
+  instantiate_quantized_aligned_batched(affine_qmm_t, type, 16, 6, false, 0) \
+  instantiate_quantized_aligned_batched(affine_qmm_t, type, 16, 6, false, 1) \
+  instantiate_quantized_splitk_qmm(affine_qmm_t_splitk, type, 16, 6, true) \
+  instantiate_quantized_splitk_qmm(affine_qmm_t_splitk, type, 16, 6, false) \
+  instantiate_quantized_aligned(affine_gather_qmm_t, type, 16, 6, true) \
+  instantiate_quantized_aligned(affine_gather_qmm_t, type, 16, 6, false)
 
 #define instantiate_q6_group16() \
   instantiate_q6_group16_type(float) \
diff --git a/mlx/backend/metal/kernels/quantized.h b/mlx/backend/metal/kernels/quantized.h
index 345dfc71..ff638b63 100644
--- a/mlx/backend/metal/kernels/quantized.h
+++ b/mlx/backend/metal/kernels/quantized.h
@@ -572,11 +572,8 @@ template <
     short bits>
 struct QuantizedBlockLoader {
   static_assert(
-      BCOLS <= group_size,
-      "The group size should be larger than the columns");
-  static_assert(
-      group_size % BCOLS == 0,
-      "The group size should be divisible by the columns");
+      BCOLS % group_size == 0 || group_size % BCOLS == 0,
+      "The group size and columns should divide evenly");
   static_assert(
       bits == 2 || bits == 3 || bits == 4 || bits == 5 || bits == 6 ||
           bits == 8,
@@ -587,7 +584,8 @@ struct QuantizedBlockLoader {
   MLX_MTL_CONST short BCOLS_PACKED = BCOLS / pack_factor;
   MLX_MTL_CONST short n_reads =
       (BCOLS_PACKED * BROWS < tgp_size) ? 1 : (BCOLS_PACKED * BROWS) / tgp_size;
-  MLX_MTL_CONST short group_steps = group_size / BCOLS;
+  MLX_MTL_CONST short group_steps =
+      group_size > BCOLS ? group_size / BCOLS : 1;
 
   const int src_ld;
   const int tile_stride;
@@ -631,9 +629,9 @@ struct QuantizedBlockLoader {
       return;
     }
 
-    T scale = *scales;
-    T bias = *biases;
     for (int i = 0; i < n_reads; i++) {
+      T scale = scales[(bj + i) * pack_factor / group_size];
+      T bias = biases[(bj + i) * pack_factor / group_size];
       dequantize<T, pack_factor, bits>(
           src + i * bytes_per_pack, scale, bias, dst + i * pack_factor);
     }
@@ -658,9 +656,9 @@ struct QuantizedBlockLoader {
       return;
     }
 
-    T scale = *scales;
-    T bias = *biases;
     for (int i = 0; i < n_reads; i++) {
+      T scale = scales[(bj + i) * pack_factor / group_size];
+      T bias = biases[(bj + i) * pack_factor / group_size];
       dequantize<T, pack_factor, bits>(
           (device uint8_t*)(src + i * bytes_per_pack),
           scale,
@@ -680,8 +678,8 @@ struct QuantizedBlockLoader {
           biases++;
         }
       } else {
-        scales++;
-        biases++;
+        scales += BCOLS >= group_size ? BCOLS / group_size : 1;
+        biases += BCOLS >= group_size ? BCOLS / group_size : 1;
       }
     } else {
       scales += group_stride;
diff --git a/mlx/backend/metal/quantized.cpp b/mlx/backend/metal/quantized.cpp
--- a/mlx/backend/metal/quantized.cpp
+++ b/mlx/backend/metal/quantized.cpp
@@ -786,7 +786,7 @@ void qmm(
     const Stream& s,
     const std::string& mode) {
-  if (metal::is_nax_available() && transpose && (K % 64 == 0) &&
+  if (metal::is_nax_available() && group_size != 16 && transpose &&
+      (K % 64 == 0) &&
       (env::enable_tf32() || x.dtype() != float32)) {
     return qmm_nax(
@@ -977,7 +978,8 @@ void gather_qmm(
     const Stream& s,
     const std::string& mode) {
-  if (metal::is_nax_available() && transpose && (K % 64 == 0) &&
+  if (metal::is_nax_available() && group_size != 16 && transpose &&
+      (K % 64 == 0) &&
       (env::enable_tf32() || x.dtype() != float32)) {
     return gather_qmm_nax(
@@ -1503,12 +1505,6 @@ void QuantizedMatmul::eval_gpu(const std::vector<array>& inputs, array& out) {
   int vector_limit = transpose_ ? get_qmv_batch_limit(K, N, d) : 4;
   auto mode = quantization_mode_to_string(mode_);
-  if (group_size_ == 16 && transpose_) {
-    qmv(
-        x, w, scales, biases, out, group_size_, bits_, M, N, K, d, s, mode);
-    return;
-  }
-
   // It is a matrix matrix product.
   if (M >= vector_limit) {
@@ -1583,24 +1579,6 @@ void GatherQMM::eval_gpu(const std::vector<array>& inputs, array& out) {
   int vector_limit = transpose_ ? get_qmv_batch_limit(K, N, d) : 4;
   auto mode = quantization_mode_to_string(mode_);
 
-  if (group_size_ == 16 && transpose_) {
-    gather_qmv(
-        x,
-        w,
-        scales,
-        biases,
-        lhs_indices,
-        rhs_indices,
-        out,
-        group_size_,
-        bits_,
-        M,
-        N,
-        K,
-        d,
-        s,
-        mode);
-    return;
-  }
-
   // We are walking x in order and w is also in order so we can batch up the
