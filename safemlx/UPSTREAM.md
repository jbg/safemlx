# Upstream Source

This crate was imported from the `mlx-rs/` crate in
[`oxiglade/mlx-rs`](https://github.com/oxiglade/mlx-rs) at commit
`f4aa309c79b6be35255ca7d34157dfc10d9ed4c9`.

The `mlx-sys` crate is vendored in this repository as `safemlx-sys`.
